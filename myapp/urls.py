from django.urls import path
from . import views

app_name = "myapp"
urlpatterns = [
    # path("url/", views.customise_url, name="url"),
    # path("<str:keyword>/", views.new_url, name="custom"),
]
